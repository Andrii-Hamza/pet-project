server:
  port: 8888
logging:
  file:
    name: services/config-server/logs/config-server.log
spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
